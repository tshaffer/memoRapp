// ------------------------------------
// Reducer
// ------------------------------------

const initialState: any = {};

export const restaurantMenuItemReducer = (
  state: any = initialState,
  action: any
): any => {
  return state;
};
